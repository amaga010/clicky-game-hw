import React from "react";
import "../components/header.css"

const Header = () => (
	<header className = "header">
		<h1>Look At All The Doggos !!</h1>
		<h2>Can you click on all the doggos without clicking the same doggo more than once ? Start clicking to find out !</h2>
	</header>
);
export default Header;
